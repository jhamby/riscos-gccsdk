# The install-minimum target will only be install the bare asasm code, not
# decaof nor elftoolchain.
install-minimum:
	$(MAKE) $(AM_MAKEFLAGS) install SUBDIRS=src

SUBDIRS = decaof elftoolchain src testing


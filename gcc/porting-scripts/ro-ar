#!/bin/bash

RO_CPARAMS=

# Check Parameters for recurse option
for param in $@ ; do
  if [ $(echo $param | cut -b 1) == "-" ] ; then
    :
  else
    RO_CPARAMS="$RO_CPARAMS $param"
  fi
done

GCCSDK_ENV/ro-path ar $RO_CPARAMS

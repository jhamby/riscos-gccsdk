--- ../../../../gcc3.4.6/gcc-3.4.6/gcc/c-incpath.c	2004-05-31 12:37:47.000000000 +0200
+++ ./c-incpath.c	2006-09-13 23:12:06.000000000 +0200
@@ -315,6 +315,20 @@
     if (*c == '\\') *c = '/';
 #endif
 
+#ifdef __riscos__
+  char *temp;
+  temp = __unixify_std(path, NULL, 0, 0, NULL);
+  if (!temp)
+    {
+      fprintf (stderr, _("Unixify failed (%s), falling back to input path\n"),
+      path);
+    }
+  else
+    {
+      free(path);
+      path = temp;
+    }
+#endif
   p = xmalloc (sizeof (struct cpp_dir));
   p->next = NULL;
   p->name = path;

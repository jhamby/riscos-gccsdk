#!/bin/bash -e
# Auto-generate libunixlib
# Copyright (c) 2007-2010 GCCSDK Developers

# Generate the Makefile.am files:
./libunixlib/gen-auto.pl

# Run the autotools:
( cd libunixlib && aclocal -I .. -I ../config && autoheader && automake -a && autoconf )

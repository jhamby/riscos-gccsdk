include		../config.mk

zip:
	$(ZIP) -,r9 ../loaders-bin.zip !DSO-libs

clean:
	rm -frv !DSO-libs/SoManager,ffa
	rm -frv !DSO-libs/loader,ff8
	rm -frv !DSO-libs/lib/system/ld-1.9.9.so,e1f
	rm -frv !DSO-libs/lib/system/libdl.1.9.9.so,e1f


AB_CATEGORY=Games

AB_PRECONF=:

. sys/unix/setup.sh

cd ../../src
CC=/usr/bin/gcc make objects.o monst.o
CC=/usr/bin/gcc make -C ../util makedefs

rm *.o
make objects.o monst.o
touch ../util/makedefs

#rm ../util/makedefs.o

ab_make() {
  $AB_MAKE WIN=console LIBS=-L/home/riscos/env/lib
}

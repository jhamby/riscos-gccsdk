AB_CATEGORY=cli

AB_PRECONF="CFLAGS=\"-DNO_ARCHIVES -DSYS_SIGLIST_DECLARED -O2\""

AB_MANPAGE="make.1"

ab_package() {
  cp $S/make$AB_EXEEXT COPYING $D
  ( cd $S ; make html )
  cp -r $S/doc/make $D/doc
}

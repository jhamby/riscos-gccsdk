
AB_CATEGORY=browser

AB_CONFLAGS=--disable-dlgui

AB_MANPAGE=debian/dillo.1

# Undo Debian patch
patch -p1 -R < debian/patches/01_i18n

ab_package() {
  cp -av $H/\!Dillo $D
  cp -av $S/src/dillo$AB_EXEEXT $D/\!Dillo
  cp -av $S/COPYING $D/\!Dillo
}

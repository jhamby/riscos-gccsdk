ab_package() 
{
  ab_create_app RockDodger Apps/Games

  ab_makerun $S/rockdodger
  cp -av COPYING $A

  mkdir -p $A/data
  cp data/*.{bmp,png,wav,mod} $A/data

  $AB_HOME/add-riscpkg -unixlib -unixsound -unixhome
}

AB_PRECONF=:

ab_package() {
  ab_create_app IceBreaker Apps/Games

  ab_makerun icebreaker 
  cat $S/README >> $A/\!Help

  cp -av $S/ChangeLog $S/LICENSE $S/TODO $S/README $A/

  mkdir -p $A/data
  cp -r $S/*.wav $S/*.bmp $A/data

  $AB_HOME/add-riscpkg -unixhome -unixlib -name IceBreaker
}

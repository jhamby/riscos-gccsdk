AB_INSTALL=yes

# We've patched src/platform/X11/Makefile.am so re-run automake
aclocal
autoconf
libtoolize --force
automake --foreign --add-missing --ignore-deps

mkdir -p $D/include/agg2
mkdir -p $D/include/agg2/ctrl
mkdir -p $D/include/agg2/platform
mkdir -p $D/include/agg2/util


AB_INSTALL=yes

AB_CONFLAGS=--disable-modules

mkdir -p $D/include/gdk-pixbuf-1.0/gdk-pixbuf/

# Debian style setup 
ln -sf $GCCSDK_INSTALL_ENV/lib/libgdk_pixbuf.a $GCCSDK_INSTALL_ENV/lib/libgdk_pixbuf-2.0.a

mkdir -p $D/lib/
ln -sf $D/lib/libgdk_pixbuf.a $D/lib/libgdk_pixbuf-2.0.a

